/* ═══════════════════════════════════════════════
   BASE.CSS — Design tokens & global styles
   ═══════════════════════════════════════════════ */

@import url('https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;1,9..40,300&family=Instrument+Serif:ital@0;1&display=swap');

/* ── Tokens ─────────────────────────────────── */
:root {
  /* Colors */
  --bg:          #0a0a0a;
  --bg-surface:  #111111;
  --bg-raised:   #1a1a1a;
  --bg-overlay:  rgba(0,0,0,0.72);
  --border:      rgba(255,255,255,0.08);
  --border-hover:rgba(255,255,255,0.18);

  --text-primary:   #f0ece4;
  --text-secondary: #888070;
  --text-muted:     #4a4540;

  --accent:      #e8c97a;       /* warm gold */
  --accent-dim:  rgba(232,201,122,0.12);
  --accent-glow: rgba(232,201,122,0.25);

  --danger:      #e05c5c;
  --success:     #5ce07a;
  --info:        #5cb8e0;

  /* Typography */
  --font-sans:   'DM Sans', sans-serif;
  --font-serif:  'Instrument Serif', serif;

  /* Spacing scale */
  --s1: 4px;   --s2: 8px;   --s3: 12px;  --s4: 16px;
  --s5: 20px;  --s6: 24px;  --s7: 32px;  --s8: 48px;

  /* Radius */
  --r-sm: 6px;  --r-md: 12px;  --r-lg: 20px;  --r-full: 9999px;

  /* Shadow */
  --shadow-sm:  0 1px 3px rgba(0,0,0,0.4);
  --shadow-md:  0 4px 16px rgba(0,0,0,0.5);
  --shadow-lg:  0 12px 40px rgba(0,0,0,0.6);
  --glow:       0 0 24px var(--accent-glow);

  /* Transitions */
  --ease: cubic-bezier(0.22, 1, 0.36, 1);
  --t-fast: 140ms var(--ease);
  --t-med:  260ms var(--ease);
  --t-slow: 420ms var(--ease);

  /* Layout */
  --sidebar-w: 240px;
  --content-max: 640px;
}

/* ── Reset ───────────────────────────────────── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html { font-size: 15px; scroll-behavior: smooth; }

body {
  font-family: var(--font-sans);
  background: var(--bg);
  color: var(--text-primary);
  line-height: 1.6;
  min-height: 100vh;
  -webkit-font-smoothing: antialiased;
}

a { color: inherit; text-decoration: none; }
img, video { max-width: 100%; display: block; }
button { font-family: inherit; cursor: pointer; border: none; background: none; }
input, textarea, select { font-family: inherit; }

/* ── Scrollbar ───────────────────────────────── */
::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--border); border-radius: 2px; }
::-webkit-scrollbar-thumb:hover { background: var(--border-hover); }

/* ── Utility classes ─────────────────────────── */
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}

.flex        { display: flex; }
.flex-col    { display: flex; flex-direction: column; }
.items-center{ align-items: center; }
.justify-between { justify-content: space-between; }
.gap-1 { gap: var(--s1); } .gap-2 { gap: var(--s2); }
.gap-3 { gap: var(--s3); } .gap-4 { gap: var(--s4); }

.text-sm   { font-size: 0.8rem; }
.text-xs   { font-size: 0.72rem; }
.text-muted{ color: var(--text-secondary); }
.font-500  { font-weight: 500; }
.font-600  { font-weight: 600; }

.truncate  { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* ── Avatar ──────────────────────────────────── */
.avatar {
  border-radius: var(--r-full);
  object-fit: cover;
  flex-shrink: 0;
  background: var(--bg-raised);
  display: flex; align-items: center; justify-content: center;
  color: var(--text-secondary);
  font-weight: 600;
  overflow: hidden;
}
.avatar-xs  { width: 28px;  height: 28px;  font-size: 0.7rem; }
.avatar-sm  { width: 36px;  height: 36px;  font-size: 0.8rem; }
.avatar-md  { width: 48px;  height: 48px;  font-size: 1rem; }
.avatar-lg  { width: 80px;  height: 80px;  font-size: 1.5rem; }
.avatar-xl  { width: 110px; height: 110px; font-size: 2rem; }

/* ── Buttons ─────────────────────────────────── */
.btn {
  display: inline-flex; align-items: center; justify-content: center;
  gap: var(--s2); padding: 9px 18px;
  border-radius: var(--r-full);
  font-size: 0.87rem; font-weight: 500;
  transition: all var(--t-fast);
  white-space: nowrap; letter-spacing: 0.01em;
}
.btn:disabled { opacity: 0.4; pointer-events: none; }

.btn-primary {
  background: var(--accent); color: #1a1400;
  box-shadow: 0 2px 12px var(--accent-glow);
}
.btn-primary:hover {
  background: #f0d68a;
  box-shadow: 0 4px 20px var(--accent-glow);
  transform: translateY(-1px);
}
.btn-primary:active { transform: translateY(0); }

.btn-ghost {
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-primary);
}
.btn-ghost:hover { border-color: var(--border-hover); background: var(--bg-raised); }

.btn-danger {
  background: rgba(224,92,92,0.12);
  border: 1px solid rgba(224,92,92,0.25);
  color: var(--danger);
}
.btn-danger:hover { background: rgba(224,92,92,0.2); }

.btn-sm { padding: 6px 14px; font-size: 0.8rem; }
.btn-icon {
  width: 36px; height: 36px; padding: 0;
  border-radius: var(--r-full);
  border: 1px solid var(--border);
}
.btn-icon:hover { background: var(--bg-raised); border-color: var(--border-hover); }

/* ── Card ────────────────────────────────────── */
.card {
  background: var(--bg-surface);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  overflow: hidden;
}

/* ── Input ───────────────────────────────────── */
.input {
  width: 100%;
  background: var(--bg-raised);
  border: 1px solid var(--border);
  border-radius: var(--r-md);
  padding: 10px 14px;
  color: var(--text-primary);
  font-size: 0.9rem;
  transition: border-color var(--t-fast), box-shadow var(--t-fast);
  outline: none;
}
.input::placeholder { color: var(--text-muted); }
.input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-dim);
}

.input-group { display: flex; flex-direction: column; gap: var(--s2); }
.input-group label { font-size: 0.82rem; font-weight: 500; color: var(--text-secondary); }

/* ── Divider ─────────────────────────────────── */
.divider {
  height: 1px;
  background: var(--border);
  margin: var(--s4) 0;
}

/* ── Badge ───────────────────────────────────── */
.badge {
  display: inline-flex; align-items: center;
  padding: 2px 8px;
  border-radius: var(--r-full);
  font-size: 0.72rem; font-weight: 600;
  background: var(--accent-dim); color: var(--accent);
}
.badge-danger { background: rgba(224,92,92,0.12); color: var(--danger); }

/* ── Toast ───────────────────────────────────── */
#toast-container {
  position: fixed; bottom: 24px; right: 24px;
  display: flex; flex-direction: column; gap: var(--s2);
  z-index: 9999;
}
.toast {
  padding: 12px 18px;
  background: var(--bg-raised);
  border: 1px solid var(--border);
  border-left: 3px solid var(--accent);
  border-radius: var(--r-md);
  font-size: 0.85rem;
  box-shadow: var(--shadow-md);
  animation: toastIn var(--t-med) var(--ease);
  max-width: 320px;
}
.toast.error { border-left-color: var(--danger); }
.toast.success { border-left-color: var(--success); }

@keyframes toastIn {
  from { opacity: 0; transform: translateX(16px); }
  to   { opacity: 1; transform: translateX(0); }
}

/* ── Modal ───────────────────────────────────── */
.modal-backdrop {
  position: fixed; inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(8px);
  display: flex; align-items: center; justify-content: center;
  z-index: 1000;
  animation: fadeIn 200ms ease;
}
.modal {
  background: var(--bg-surface);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  padding: var(--s7);
  width: min(480px, calc(100vw - 32px));
  box-shadow: var(--shadow-lg);
  animation: slideUp var(--t-med) var(--ease);
}
.modal-header {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: var(--s5);
}
.modal-title { font-family: var(--font-serif); font-size: 1.3rem; }

@keyframes fadeIn  { from { opacity: 0; } to { opacity: 1; } }
@keyframes slideUp { from { opacity:0; transform: translateY(20px); } to { opacity:1; transform: translateY(0); } }

/* ── Spinner ─────────────────────────────────── */
.spinner {
  width: 20px; height: 20px;
  border: 2px solid var(--border);
  border-top-color: var(--accent);
  border-radius: var(--r-full);
  animation: spin 0.6s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* ── Empty state ─────────────────────────────── */
.empty-state {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  gap: var(--s3); padding: var(--s8);
  color: var(--text-muted); text-align: center;
}
.empty-state svg { opacity: 0.3; }

/* ── Responsive helpers ──────────────────────── */
@media (max-width: 768px) {
  :root { --sidebar-w: 0px; }
  .hide-mobile { display: none !important; }
}
/* ═══════════════════════════════════════════════
   COMPONENTS.CSS — Posts, threads, stories, profiles
   ═══════════════════════════════════════════════ */

/* ── Stories Bar ─────────────────────────────── */
.stories-bar {
  display: flex; gap: var(--s4);
  overflow-x: auto; padding: var(--s4) 0;
  scrollbar-width: none;
}
.stories-bar::-webkit-scrollbar { display: none; }

.story-item {
  display: flex; flex-direction: column; align-items: center; gap: var(--s1);
  flex-shrink: 0; cursor: pointer;
}
.story-ring {
  padding: 2px;
  border-radius: var(--r-full);
  background: conic-gradient(var(--accent) 0%, #e8c97a88 60%, transparent 80%);
}
.story-ring.seen {
  background: conic-gradient(var(--text-muted) 0%, var(--text-muted) 100%);
}
.story-avatar-wrap {
  padding: 2px;
  background: var(--bg-surface);
  border-radius: var(--r-full);
}
.story-label {
  font-size: 0.72rem; color: var(--text-secondary);
  max-width: 60px; text-align: center;
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
}

/* ── Post Card ───────────────────────────────── */
.post-card {
  background: var(--bg-surface);
  border: 1px solid var(--border);
  border-radius: var(--r-lg);
  overflow: hidden;
  transition: border-color var(--t-fast);
}
.post-card:hover { border-color: var(--border-hover); }

.post-header {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s4);
}
.post-header-info { flex: 1; min-width: 0; }
.post-author { font-size: 0.9rem; font-weight: 600; }
.post-location { font-size: 0.78rem; color: var(--text-secondary); }
.post-menu { color: var(--text-muted); padding: var(--s2); border-radius: var(--r-sm); transition: background var(--t-fast); }
.post-menu:hover { background: var(--bg-raised); color: var(--text-primary); }

/* Media */
.post-media { position: relative; background: var(--bg-raised); }
.post-media img,
.post-media video {
  width: 100%; max-height: 600px;
  object-fit: cover;
  display: block;
}

.carousel-dots {
  position: absolute; bottom: var(--s3);
  left: 50%; transform: translateX(-50%);
  display: flex; gap: var(--s1);
}
.carousel-dot {
  width: 6px; height: 6px;
  border-radius: var(--r-full);
  background: rgba(255,255,255,0.4);
  transition: background var(--t-fast), width var(--t-fast);
}
.carousel-dot.active { width: 18px; background: #fff; }

.carousel-btn {
  position: absolute; top: 50%; transform: translateY(-50%);
  width: 32px; height: 32px;
  background: rgba(0,0,0,0.55); backdrop-filter: blur(4px);
  border-radius: var(--r-full); color: #fff;
  display: flex; align-items: center; justify-content: center;
  font-size: 1rem; transition: background var(--t-fast);
}
.carousel-btn:hover { background: rgba(0,0,0,0.75); }
.carousel-btn.prev { left: var(--s3); }
.carousel-btn.next { right: var(--s3); }

/* Actions */
.post-actions {
  display: flex; align-items: center; gap: var(--s1);
  padding: var(--s3) var(--s4) var(--s2);
}
.action-btn {
  display: flex; align-items: center; gap: 6px;
  padding: var(--s2) var(--s3);
  border-radius: var(--r-md);
  color: var(--text-secondary);
  font-size: 0.85rem;
  transition: all var(--t-fast);
}
.action-btn:hover { background: var(--bg-raised); color: var(--text-primary); }
.action-btn.liked { color: var(--danger); }
.action-btn.liked svg { fill: var(--danger); }
.action-btn.saved { color: var(--accent); }

.action-btn-save { margin-left: auto; }

/* Post Body */
.post-body { padding: 0 var(--s4) var(--s4); }
.post-likes { font-size: 0.85rem; font-weight: 600; margin-bottom: var(--s2); }

.post-caption {
  font-size: 0.88rem; line-height: 1.55;
  color: var(--text-primary);
}
.post-caption .author-tag { font-weight: 600; }
.post-caption .hashtag    { color: var(--accent); cursor: pointer; }
.post-caption .hashtag:hover { text-decoration: underline; }

.post-comments-preview {
  margin-top: var(--s3);
  display: flex; flex-direction: column; gap: var(--s2);
}
.comment-preview-item { font-size: 0.85rem; }
.comment-preview-item .author { font-weight: 600; margin-right: var(--s1); }
.view-comments-btn {
  font-size: 0.82rem; color: var(--text-muted);
  margin-top: var(--s1); cursor: pointer;
  transition: color var(--t-fast);
}
.view-comments-btn:hover { color: var(--text-secondary); }

.post-timestamp {
  font-size: 0.75rem; color: var(--text-muted);
  margin-top: var(--s3); letter-spacing: 0.01em;
}

/* Comment Input */
.post-comment-input {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s3) var(--s4);
  border-top: 1px solid var(--border);
}
.comment-field {
  flex: 1; background: transparent; border: none;
  color: var(--text-primary); font-size: 0.88rem; outline: none;
  resize: none;
}
.comment-field::placeholder { color: var(--text-muted); }
.comment-submit {
  font-size: 0.85rem; font-weight: 600; color: var(--accent);
  transition: opacity var(--t-fast); cursor: pointer;
}
.comment-submit:hover { opacity: 0.75; }
.comment-submit:disabled { opacity: 0.3; pointer-events: none; }

/* ── Feed Layout ─────────────────────────────── */
.feed {
  display: flex; flex-direction: column; gap: var(--s4);
  max-width: var(--content-max); margin: 0 auto;
}

/* ── Feed Tabs ───────────────────────────────── */
.feed-tabs {
  display: flex; gap: 0;
  border-bottom: 1px solid var(--border);
  margin-bottom: var(--s4);
}
.feed-tab {
  padding: var(--s3) var(--s5);
  font-size: 0.85rem; font-weight: 500;
  color: var(--text-muted);
  border-bottom: 2px solid transparent;
  margin-bottom: -1px;
  transition: all var(--t-fast);
}
.feed-tab:hover { color: var(--text-secondary); }
.feed-tab.active { color: var(--accent); border-bottom-color: var(--accent); }

/* ── Profile Page ────────────────────────────── */
.profile-header {
  padding: var(--s7) var(--s6);
  display: flex; gap: var(--s7);
  align-items: flex-start;
}
.profile-avatar-wrap { position: relative; }
.profile-avatar-wrap .story-ring { display: inline-block; }

.profile-info { flex: 1; }
.profile-username-row {
  display: flex; align-items: center; gap: var(--s4); margin-bottom: var(--s4); flex-wrap: wrap;
}
.profile-username {
  font-size: 1.4rem; font-weight: 400;
  font-family: var(--font-serif);
  letter-spacing: -0.01em;
}

.profile-stats {
  display: flex; gap: var(--s6); margin-bottom: var(--s4);
}
.profile-stat { display: flex; flex-direction: column; align-items: center; gap: 1px; cursor: pointer; }
.profile-stat-num { font-size: 1.05rem; font-weight: 600; }
.profile-stat-label { font-size: 0.78rem; color: var(--text-secondary); }
.profile-stat:hover .profile-stat-num { color: var(--accent); }

.profile-bio { font-size: 0.88rem; line-height: 1.55; max-width: 440px; }
.profile-bio .profile-name { font-weight: 600; display: block; margin-bottom: 4px; }
.profile-website { font-size: 0.85rem; color: var(--accent); margin-top: var(--s2); }

/* Post Grid */
.post-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 3px;
}
.post-grid-item {
  position: relative; aspect-ratio: 1/1;
  overflow: hidden; cursor: pointer;
}
.post-grid-item img {
  width: 100%; height: 100%; object-fit: cover;
  transition: transform var(--t-med);
}
.post-grid-item:hover img { transform: scale(1.05); }
.post-grid-overlay {
  position: absolute; inset: 0;
  background: rgba(0,0,0,0.45);
  display: flex; align-items: center; justify-content: center;
  gap: var(--s5);
  opacity: 0; transition: opacity var(--t-fast);
}
.post-grid-item:hover .post-grid-overlay { opacity: 1; }
.grid-stat { display: flex; align-items: center; gap: 6px; color: #fff; font-weight: 600; font-size: 0.9rem; }

/* ── Messaging ───────────────────────────────── */
.messages-layout {
  display: grid;
  grid-template-columns: 320px 1fr;
  height: calc(100vh - 0px);
  overflow: hidden;
}

.threads-list {
  border-right: 1px solid var(--border);
  display: flex; flex-direction: column;
  overflow: hidden;
}
.threads-header {
  padding: var(--s4) var(--s5);
  border-bottom: 1px solid var(--border);
  display: flex; align-items: center; justify-content: space-between;
}
.threads-title { font-family: var(--font-serif); font-size: 1.2rem; }

.threads-search {
  padding: var(--s3) var(--s4);
  border-bottom: 1px solid var(--border);
}
.threads-search .input { padding: 8px 12px; font-size: 0.85rem; }

.thread-items { overflow-y: auto; flex: 1; }

.thread-item {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s3) var(--s4);
  cursor: pointer; transition: background var(--t-fast);
  position: relative;
}
.thread-item:hover  { background: var(--bg-raised); }
.thread-item.active { background: var(--accent-dim); }

.thread-info { flex: 1; min-width: 0; }
.thread-name {
  font-size: 0.88rem; font-weight: 500;
  display: flex; align-items: center; justify-content: space-between;
}
.thread-time { font-size: 0.75rem; color: var(--text-muted); font-weight: 400; }
.thread-preview {
  font-size: 0.8rem; color: var(--text-muted);
  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;
  margin-top: 2px;
}
.thread-preview.unread { color: var(--text-primary); font-weight: 500; }

.thread-unread-dot {
  width: 8px; height: 8px;
  background: var(--accent);
  border-radius: var(--r-full);
  flex-shrink: 0;
}

/* Chat Window */
.chat-window {
  display: flex; flex-direction: column; overflow: hidden;
}
.chat-header {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s4) var(--s5);
  border-bottom: 1px solid var(--border);
}
.chat-title { font-weight: 600; font-size: 0.95rem; }
.chat-subtitle { font-size: 0.78rem; color: var(--text-muted); }
.chat-header-actions { margin-left: auto; display: flex; gap: var(--s2); }

.messages-area {
  flex: 1; overflow-y: auto; padding: var(--s4) var(--s5);
  display: flex; flex-direction: column; gap: var(--s3);
}

.msg-group { display: flex; flex-direction: column; gap: 3px; }
.msg-group.outgoing { align-items: flex-end; }
.msg-group.incoming { align-items: flex-start; }

.msg-row { display: flex; align-items: flex-end; gap: var(--s2); max-width: 70%; }
.msg-group.outgoing .msg-row { flex-direction: row-reverse; }

.msg-bubble {
  padding: 9px 14px;
  border-radius: 18px;
  font-size: 0.88rem; line-height: 1.5;
  word-break: break-word;
}
.msg-group.incoming .msg-bubble {
  background: var(--bg-raised);
  border-bottom-left-radius: 4px;
}
.msg-group.outgoing .msg-bubble {
  background: var(--accent);
  color: #1a1400; font-weight: 500;
  border-bottom-right-radius: 4px;
}

.msg-meta {
  font-size: 0.7rem; color: var(--text-muted);
  padding: 0 var(--s2); margin-top: 1px;
}

.msg-image { border-radius: var(--r-md); max-width: 240px; overflow: hidden; cursor: zoom-in; }
.msg-image img { width: 100%; object-fit: cover; }

.msg-reactions {
  display: flex; flex-wrap: wrap; gap: 4px; margin-top: 4px;
}
.reaction-pill {
  display: inline-flex; align-items: center; gap: 3px;
  padding: 2px 8px;
  background: var(--bg-raised);
  border: 1px solid var(--border);
  border-radius: var(--r-full);
  font-size: 0.78rem; cursor: pointer;
  transition: border-color var(--t-fast);
}
.reaction-pill:hover { border-color: var(--accent); }
.reaction-pill.own   { border-color: var(--accent); background: var(--accent-dim); }

.day-separator {
  display: flex; align-items: center; gap: var(--s3);
  color: var(--text-muted); font-size: 0.75rem; margin: var(--s2) 0;
}
.day-separator::before,
.day-separator::after { content:''; flex:1; height:1px; background: var(--border); }

/* Chat Input */
.chat-input-area {
  padding: var(--s3) var(--s5);
  border-top: 1px solid var(--border);
  display: flex; align-items: flex-end; gap: var(--s2);
}
.chat-input-wrap {
  flex: 1;
  background: var(--bg-raised);
  border: 1px solid var(--border);
  border-radius: 22px;
  display: flex; align-items: flex-end;
  padding: 10px 14px;
  transition: border-color var(--t-fast);
}
.chat-input-wrap:focus-within { border-color: var(--accent); }
.chat-textarea {
  flex: 1; background: transparent; border: none; outline: none;
  color: var(--text-primary); font-size: 0.9rem; resize: none;
  max-height: 120px; line-height: 1.45;
}
.chat-textarea::placeholder { color: var(--text-muted); }
.chat-emoji-btn { color: var(--text-muted); font-size: 1.2rem; transition: color var(--t-fast); }
.chat-emoji-btn:hover { color: var(--accent); }
.chat-send-btn {
  width: 40px; height: 40px;
  background: var(--accent); color: #1a1400;
  border-radius: var(--r-full);
  display: flex; align-items: center; justify-content: center;
  font-size: 1rem;
  transition: all var(--t-fast);
  flex-shrink: 0;
}
.chat-send-btn:hover { background: #f0d68a; transform: scale(1.05); }
.chat-send-btn:disabled { opacity: 0.35; transform: none; }

@media (max-width: 768px) {
  .messages-layout { grid-template-columns: 1fr; }
  .threads-list { display: none; }
  .threads-list.show { display: flex; position: fixed; inset: 0; z-index: 200; background: var(--bg); }
  .profile-header { flex-direction: column; align-items: center; text-align: center; padding: var(--s5); }
  .profile-stats  { justify-content: center; }
}

/* ── Comment Thread ──────────────────────────── */
.comment-item {
  display: flex; gap: var(--s3);
  padding: var(--s3) 0;
}
.comment-body { flex: 1; }
.comment-author { font-size: 0.88rem; font-weight: 600; }
.comment-text   { font-size: 0.88rem; line-height: 1.5; margin-top: 2px; }
.comment-actions {
  display: flex; gap: var(--s3); margin-top: var(--s2);
  align-items: center;
}
.comment-action {
  font-size: 0.78rem; color: var(--text-muted);
  transition: color var(--t-fast); cursor: pointer;
}
.comment-action:hover { color: var(--text-primary); }
.comment-action.liked { color: var(--danger); }

.comment-replies {
  margin-top: var(--s3); padding-left: var(--s5);
  border-left: 2px solid var(--border);
  display: flex; flex-direction: column; gap: 0;
}

/* ── Notification Item ───────────────────────── */
.notif-item {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s3) var(--s4);
  border-radius: var(--r-md);
  transition: background var(--t-fast);
  cursor: pointer;
}
.notif-item:hover { background: var(--bg-raised); }
.notif-item.unread { background: var(--accent-dim); }
.notif-text { flex: 1; font-size: 0.87rem; line-height: 1.45; }
.notif-text strong { font-weight: 600; }
.notif-time { font-size: 0.75rem; color: var(--text-muted); flex-shrink: 0; }
.notif-thumb {
  width: 44px; height: 44px;
  border-radius: var(--r-sm); object-fit: cover;
  background: var(--bg-raised); flex-shrink: 0;
}

/* ── User Follow Card ────────────────────────── */
.user-follow-card {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s3) 0;
}
.user-follow-info { flex: 1; min-width: 0; }
.user-follow-name { font-size: 0.88rem; font-weight: 600; }
.user-follow-meta { font-size: 0.78rem; color: var(--text-muted); }
/* ═══════════════════════════════════════════════
   LAYOUT.CSS — App shell, sidebar, nav
   ═══════════════════════════════════════════════ */

/* ── App Shell ───────────────────────────────── */
.app-shell {
  display: grid;
  grid-template-columns: var(--sidebar-w) 1fr;
  min-height: 100vh;
}

/* ── Sidebar ─────────────────────────────────── */
.sidebar {
  position: fixed;
  top: 0; left: 0;
  width: var(--sidebar-w);
  height: 100vh;
  background: var(--bg-surface);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  padding: var(--s6) var(--s4);
  z-index: 100;
  overflow-y: auto;
}

.sidebar-brand {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s3) var(--s3) var(--s6);
}
.sidebar-brand-icon {
  width: 34px; height: 34px;
  background: var(--accent);
  border-radius: var(--r-md);
  display: flex; align-items: center; justify-content: center;
  color: #1a1400; font-weight: 700; font-size: 1rem;
}
.sidebar-brand-name {
  font-family: var(--font-serif);
  font-size: 1.3rem;
  letter-spacing: -0.01em;
}

.nav-section {
  display: flex; flex-direction: column; gap: var(--s1);
  flex: 1;
}

.nav-section-label {
  font-size: 0.68rem; font-weight: 600; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--text-muted);
  padding: var(--s4) var(--s3) var(--s2);
}

.nav-link {
  display: flex; align-items: center; gap: var(--s3);
  padding: 10px var(--s3);
  border-radius: var(--r-md);
  color: var(--text-secondary);
  font-size: 0.9rem; font-weight: 400;
  transition: all var(--t-fast);
  position: relative;
}
.nav-link:hover {
  background: var(--bg-raised);
  color: var(--text-primary);
}
.nav-link.active {
  background: var(--accent-dim);
  color: var(--accent);
  font-weight: 500;
}
.nav-link .nav-icon {
  width: 20px; text-align: center;
  opacity: 0.7; transition: opacity var(--t-fast);
  font-size: 1.05rem;
}
.nav-link:hover .nav-icon,
.nav-link.active .nav-icon { opacity: 1; }

.nav-badge {
  margin-left: auto;
  background: var(--accent);
  color: #1a1400;
  font-size: 0.68rem; font-weight: 700;
  padding: 1px 6px;
  border-radius: var(--r-full);
  min-width: 18px; text-align: center;
}

/* ── Sidebar User Card ───────────────────────── */
.sidebar-user {
  display: flex; align-items: center; gap: var(--s3);
  padding: var(--s3);
  border-top: 1px solid var(--border);
  margin-top: auto;
  border-radius: var(--r-md);
  cursor: pointer;
  transition: background var(--t-fast);
}
.sidebar-user:hover { background: var(--bg-raised); }
.sidebar-user-info { flex: 1; min-width: 0; }
.sidebar-user-name  { font-size: 0.88rem; font-weight: 500; }
.sidebar-user-handle{ font-size: 0.77rem; color: var(--text-muted); }
.sidebar-user-more  { color: var(--text-muted); font-size: 1.1rem; }

/* ── Main Content ────────────────────────────── */
.main-content {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  min-width: 0; /* prevent grid blowout */
  overflow-x: hidden;
}

.page-header {
  position: sticky; top: 0; z-index: 50;
  background: rgba(10,10,10,0.85);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
  padding: var(--s4) var(--s7);
  display: flex; align-items: center; gap: var(--s4);
}
.page-title {
  font-family: var(--font-serif);
  font-size: 1.35rem;
  letter-spacing: -0.01em;
}

.page-body {
  max-width: var(--content-max);
  width: 100%;
  margin: 0 auto;
  padding: var(--s6) var(--s5);
}

.page-body-full {
  width: 100%;
  padding: var(--s6) var(--s6);
}

.page-body-wide {
  padding: var(--s6) var(--s7);
}

/* ── Mobile Top Bar ──────────────────────────── */
.mobile-topbar {
  display: none;
  position: sticky; top: 0; z-index: 100;
  background: var(--bg-surface);
  border-bottom: 1px solid var(--border);
  padding: var(--s3) var(--s4);
  align-items: center; justify-content: space-between;
}

.mobile-bottomnav {
  display: none;
  position: fixed; bottom: 0; left: 0; right: 0; z-index: 100;
  background: var(--bg-surface);
  border-top: 1px solid var(--border);
  padding: var(--s2) 0 env(safe-area-inset-bottom);
}
.mobile-bottomnav-inner {
  display: flex;
  align-items: center; justify-content: space-around;
}
.mobile-nav-btn {
  display: flex; flex-direction: column; align-items: center; gap: 2px;
  padding: var(--s2) var(--s3);
  color: var(--text-muted);
  font-size: 0.65rem; font-weight: 500;
  border-radius: var(--r-md);
  transition: color var(--t-fast);
}
.mobile-nav-btn.active { color: var(--accent); }
.mobile-nav-btn svg { width: 22px; height: 22px; }

@media (max-width: 768px) {
  .app-shell { grid-template-columns: 1fr; }
  .sidebar    { display: none; }
  .main-content { margin-left: 0; padding-bottom: 64px; }
  .mobile-topbar    { display: flex; }
  .mobile-bottomnav { display: block; }
  .page-body  { padding: var(--s4); }
}